#include <iostream>
using namespace std;

int main (){
 	int nilai;
	cout<<"masukkan nilai = ";
	cin>>nilai;
	if ((nilai >= 85) && (nilai =100))
	{
		cout<<"Nilia : A";
	}else if((nilai >= 75) && (nilai < 85))
	{
		cout<<"Nilai : B";
	}else if((nilai >= 60) && (nilai < 75))
	{
		cout<<"Nilai: C";
	}
return 0;}
